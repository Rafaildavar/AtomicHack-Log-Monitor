# 🚀 AtomicHack Log Monitor Bot

**Интеллектуальный анализатор логов с ML** для кейса «ИИ-анализатор журналов событий приложений» хакатона AtomicHack.

## 🎯 Ключевые возможности

### ✅ Полностью реализованная функциональность:

#### 🤖 **Telegram бот**
- Загрузка файлов: `txt`, `log`, `csv`, `zip` архивы до 50MB
- Интуитивная клавиатура с командами
- Умная обработка ошибок и валидация
- Асинхронная обработка файлов

#### 🧠 **ML-модуль анализа аномалий**
- **SentenceTransformers** (all-MiniLM-L6-v2) для семантического анализа
- **Косинусное сходство** для определения релевантности аномалий
- **Автоматическая классификация** проблем по уровню критичности
- **Порог уверенности 0.7** для точной идентификации

#### 📊 **Интеллектуальный анализ логов**
- Автоматическое обнаружение WARNING/ERROR сообщений
- Сопоставление аномалий с известными проблемами
- Поддержка множественных сценариев в одном архиве
- Трекинг локации проблем (файл + номер строки)

#### 📈 **Excel отчеты с детализацией**
- Автоматическое создание структурированных отчетов
- Полная информация о каждой найденной проблеме
- Метрики уверенности ML-анализа
- Статистика по сценариям и аномалиям

## 🚀 Быстрый старт

### 1. **Установка зависимостей**
```bash
cd /path/to/project
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate на Windows
pip install -r requirements.txt
```

### 2. **Настройка конфигурации**
Создайте файл `.env` с токеном бота:
```env
BOT_TOKEN=your_telegram_bot_token_here
```

### 3. **Запуск бота**
```bash
python -m src.bot.main
```

### 4. **Использование в Telegram**
1. Откройте бота в Telegram
2. Нажмите "Загрузить логи"
3. Отправьте ZIP архив с логами и файлами `anomalies_problems.csv`
4. Получите Excel отчет с ML-анализом

## 📋 Детальный алгоритм работы

### 🔄 **Шаг 1: Загрузка и валидация**
```python
# Бот принимает файл и проверяет его размер
if document.file_size > 50 * 1024 * 1024:
    return "❌ Файл слишком большой"
```

**Поддерживаемые форматы:**
- `.txt`, `.log` — текстовые логи
- `.csv` — табличные данные
- `.zip` — архивы с множественными файлами

### 🔍 **Шаг 2: Поиск словарей аномалий**
```python
# Автоматическое обнаружение anomalies_problems.csv в архиве
for root, dirs, files in os.walk(extract_dir):
    if 'anomalies_problems.csv' in files:
        scenario_name = os.path.basename(os.path.dirname(file_path))
        # Загружаем словарь аномалий для сценария
        anomalies_dict = pd.read_csv(anomalies_file, sep=';')
```

**Формат словаря аномалий:**
| ID аномалии | Аномалия | ID проблемы | Проблема |
|-------------|----------|-------------|----------|
| 267 | Authentication token validation timeout | 34 | Network interface eth0 link failure |

### 🧠 **Шаг 3: ML-анализ с трансформерами**
```python
# Загрузка модели SentenceTransformer
model = SentenceTransformer("all-MiniLM-L6-v2")

# Создание эмбеддингов для известных аномалий
known_anomalies = anomalies_dict["Аномалия"].tolist()
anomaly_embeddings = model.encode(known_anomalies, normalize_embeddings=True)

# Анализ каждой WARNING строки
for log_entry in warning_logs:
    # Получаем эмбеддинг текущей строки
    text_embedding = model.encode(log_entry["text"], normalize_embeddings=True)
    
    # Вычисляем косинусное сходство
    cosine_scores = util.cos_sim(text_embedding, anomaly_embeddings)[0]
    best_score = cosine_scores.max().item()
    
    if best_score >= 0.7:  # Порог уверенности
        # Находим соответствующую аномалию и её проблемы
        matched_anomaly = anomalies_dict.iloc[best_idx]
        # Ищем связанные ERROR строки (проблемы)
        ...
```

**Логика сопоставления:**
1. **WARNING → Аномалия** (семантический поиск с ML)
2. **Аномалия → Проблема** (по словарю)
3. **Проблема → ERROR** (точное совпадение текста)

### 📊 **Шаг 4: Создание Excel отчета**
```python
# Формирование результата для каждой найденной проблемы
result = {
    'Сценарий': scenario_name,
    'ID аномалии': anomaly_id,
    'Аномалия': warning_text,
    'Уверенность': round(best_score, 3),  # 0.0 - 1.0
    'ID проблемы': problem_id,
    'Файл с проблемой': error_filename,
    '№ строки': error_line_number,
    'Строка из лога': error_text,
    'Статус': 'Найдена проблема'
}

# Экспорт в Excel с форматированием
file_analyzer.create_excel_report(all_results)
```

### 📈 **Шаг 5: Отправка результатов**
```python
# Отправка Excel файла пользователю
await message.answer_document(
    excel_file,
    caption=f"📊 ML анализ завершен!\n"
            f"🚨 Найдено проблем: {total_problems}\n"
            f"📈 Обработано сценариев: {total_scenarios}"
)
```

## 📊 Формат выходных данных

### Excel отчет содержит:

| Сценарий | ID аномалии | Аномалия | Уверенность | ID проблемы | Файл с проблемой | № строки | Строка из лога | Статус |
|----------|-------------|----------|-------------|-------------|------------------|----------|----------------|--------|
| ValidationCase 1 | 267 | Authentication token validation timeout | 0.92 | 34 | web_server_log.txt | 15432 | ERROR: Network interface eth0 link failure | Найдена проблема |
| ValidationCase 1 | 143 | Database query optimization threshold exceeded | 0.88 | 22 | app_server_log.txt | 8765 | ERROR: Connection pool exhausted | Найдена проблема |

**Колонки отчета:**
- **Сценарий** — название папки с логами
- **ID аномалии** — уникальный идентификатор из словаря
- **Аномалия** — текст WARNING сообщения
- **Уверенность** — оценка ML модели (0.0-1.0)
- **ID проблемы** — идентификатор проблемы из словаря
- **Файл с проблемой** — имя файла с ERROR
- **№ строки** — точная локация в файле
- **Строка из лога** — полный текст ERROR сообщения
- **Статус** — результат анализа

## 🛠 Технические характеристики

### 🔧 **Технологический стек**
- **Python** 3.13+
- **aiogram** 3.22.0 — асинхронный Telegram bot framework
- **sentence-transformers** 5.1.1 — ML модели для NLP
- **PyTorch** 2.8.0 — backend для трансформеров
- **pandas** 2.3.3 — обработка данных
- **openpyxl** 3.1.5 — создание Excel отчетов

### ⚡ **Производительность**
- **Анализ логов**: ~30 секунд на 1.5 млн строк
- **ML инференс**: ~0.1 секунды на WARNING строку
- **Максимальный размер файла**: 50MB
- **Параллельная обработка**: множественные сценарии

### 🧠 **ML модель**
- **Архитектура**: all-MiniLM-L6-v2 (384-dim embeddings)
- **Метрика**: Косинусное сходство
- **Порог уверенности**: 0.7
- **Нормализация**: L2-normalized embeddings

### 📦 **Структура проекта**
```
src/
├── bot/
│   ├── handlers/
│   │   ├── upload.py          # Обработка загрузки файлов
│   │   ├── menu.py            # Меню и команды
│   │   └── start.py           # Стартовый хендлер
│   ├── services/
│   │   ├── ml_log_analyzer.py # ML-анализ логов с трансформерами
│   │   ├── analysis_history.py # Создание Excel отчетов
│   │   └── log_parser.py      # Парсинг логов
│   ├── keyboards/
│   │   └── main.py            # Клавиатуры бота
│   ├── config.py              # Конфигурация
│   └── main.py                # Точка входа
└── log_analysis/
    ├── chain_with_transformer.py # Оригинальный ML модуль
    ├── log_to_df.py           # Парсер логов в DataFrame
    └── result_df.py           # Анализатор аномалий
```

## 🎮 Команды бота

### Основные команды:
- **`/start`** — приветствие и главное меню
- **`/help`** — подробная справка по функциям
- **Кнопка "Загрузить логи"** — начать процесс анализа
- **Кнопка "Справка"** — информация о возможностях

## 🔬 Примеры использования

### Пример 1: Анализ одного сценария
```
Входные данные:
├── ValidationCase 1/
│   ├── anomalies_problems.csv
│   ├── web_server_log.txt
│   └── app_server_log.txt

Результат:
✅ ML анализ завершен!
📈 Обработано сценариев: 1
🚨 Найдено проблем: 15
📊 Средняя уверенность: 0.89
```

### Пример 2: Множественные сценарии
```
Входные данные:
├── ValidationCase 1/
│   ├── anomalies_problems.csv
│   └── logs.txt
├── ValidationCase 2/
│   ├── anomalies_problems.csv
│   └── logs.txt
└── ValidationCase 3/
    ├── anomalies_problems.csv
    └── logs.txt

Результат:
✅ ML анализ завершен!
📈 Обработано сценариев: 3
🚨 Найдено проблем: 47
📊 Средняя уверенность: 0.86
```

## 🎯 Преимущества решения

### 🚀 **Скорость и эффективность**
- Асинхронная обработка файлов
- Векторизованные операции с pandas
- Пакетная обработка эмбеддингов

### 🧠 **Точность ML-анализа**
- Семантический поиск вместо точного совпадения
- Устойчивость к опечаткам и вариациям текста
- Адаптация к новым типам аномалий

### 📊 **Удобство использования**
- Простой Telegram интерфейс
- Автоматическая обработка архивов
- Подробные Excel отчеты

### 🔧 **Гибкость и расширяемость**
- Поддержка множественных сценариев
- Настраиваемый порог уверенности
- Легко добавить новые типы анализа

## 📞 Контакты команды

**Проект создан в рамках хакатона AtomicHack**

**GitHub:** https://github.com/Rafaildavar/AtomicHack-Log-Monitor

---

## 📄 Лицензия

Проект создаётся в рамках хакатона AtomicHack; условия использования определяются оргкомитетом.

**Готов к демонстрации и защите! 🎯✨**
